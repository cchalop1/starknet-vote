const CreateProposal = () => {
  return <div>
    <input type="text" placeholder="What are your favorite project on Starknet ?" className="w-full" />
    {/* TODO: list of options */}
  </div>;
};

export default CreateProposal;
